// Não é possivel criar variaveis com palavras reservadas pela linguagem.
// Variaveis com cammelCase -> nomeCompleto = Felipe Castro Borges

const nome = "Felipe"; //Variaveis com const não podem ser alteradas.

//operaçoes: (soma = +), (subtração = -), (multiplicação = *), (divisão = /)
const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
let resultadoDobrado = resultado * 2;
resultadoDobrado = resultadoDobrado + 5;
console.log(resultadoDobrado);

//Como saber o tipo
console.log(typeof primeiroNumero);
